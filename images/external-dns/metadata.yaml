name: external-dns
track: wolfi
description: Kubernetes DNS record controller 0.19.0 built from a pinned upstream source commit.
upstream: https://github.com/kubernetes-sigs/external-dns
versions: [0.19.0]
owner: tektum
enabled: true
