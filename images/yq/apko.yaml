contents:
  repositories:
    - https://packages.wolfi.dev/os
  keyring:
    - https://packages.wolfi.dev/os/wolfi-signing.rsa.pub
  packages:
    - yq

accounts:
  run-as: 65532

entrypoint:
  command: /usr/bin/yq

environment:
  PATH: /usr/bin:/bin
