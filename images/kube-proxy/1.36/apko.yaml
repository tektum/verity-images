contents:
  repositories:
    - https://packages.wolfi.dev/os
  keyring:
    - https://packages.wolfi.dev/os/wolfi-signing.rsa.pub
  packages:
    - ca-certificates-bundle
    - kube-proxy-1.36-default

entrypoint:
  command: /usr/bin/kube-proxy

environment:
  PATH: /usr/bin:/bin
