contents:
  repositories:
    - https://packages.wolfi.dev/os
  keyring:
    - https://packages.wolfi.dev/os/wolfi-signing.rsa.pub
  packages:
    - ca-certificates-bundle
    - opa

accounts:
  run-as: 65532

entrypoint:
  command: /usr/bin/opa

environment:
  PATH: /usr/bin:/bin
