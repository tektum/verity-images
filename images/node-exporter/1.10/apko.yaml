contents:
  repositories:
    - https://packages.wolfi.dev/os
    - "@local @LOCAL_REPOSITORY@"
  keyring:
    - https://packages.wolfi.dev/os/wolfi-signing.rsa.pub
    - "@LOCAL_KEY@"
  packages:
    - prometheus-node-exporter@local

accounts:
  run-as: 65532

entrypoint:
  command: /bin/node_exporter

environment:
  PATH: /usr/bin:/bin
