contents:
  repositories:
    - https://packages.wolfi.dev/os
  keyring:
    - https://packages.wolfi.dev/os/wolfi-signing.rsa.pub
  packages:
    - ca-certificates-bundle
    - kube-controller-manager-1.36-default

accounts:
  run-as: 65532

entrypoint:
  command: /usr/bin/kube-controller-manager

environment:
  PATH: /usr/bin:/bin
