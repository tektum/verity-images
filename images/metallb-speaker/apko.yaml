contents:
  repositories:
    - https://packages.wolfi.dev/os
    - "@local @LOCAL_REPOSITORY@"
  keyring:
    - https://packages.wolfi.dev/os/wolfi-signing.rsa.pub
    - "@LOCAL_KEY@"
  packages:
    - ca-certificates-bundle
    - verity-metallb-speaker@local

accounts:
  run-as: 65534

entrypoint:
  command: /speaker

environment:
  PATH: /usr/bin:/bin
