contents:
  repositories:
    - https://packages.wolfi.dev/os
  keyring:
    - https://packages.wolfi.dev/os/wolfi-signing.rsa.pub
  packages:
    - cosign~2

accounts:
  run-as: 65532

entrypoint:
  command: /usr/bin/cosign

environment:
  PATH: /usr/bin:/bin
