package:
  name: haproxy
  version: "3.4.4"
  epoch: 0
  description: Reliable, high performance TCP/HTTP load balancer
  copyright:
    - license: GPL-2.0-or-later
  dependencies:
    runtime:
      - libssl3
      - pcre2
      - zlib

environment:
  contents:
    repositories:
      - https://packages.wolfi.dev/os
    keyring:
      - https://packages.wolfi.dev/os/wolfi-signing.rsa.pub
    packages:
      - build-base
      - busybox
      - linux-headers
      - openssl-dev
      - pcre2-dev
      - zlib-dev

pipeline:
  - uses: fetch
    with:
      uri: https://www.haproxy.org/download/3.4/src/haproxy-${{package.version}}.tar.gz
      expected-sha256: b0c5053c4d46840ecdee3925736fe9a3de6472559b43c69183d70e593d9133df

  - runs: |
      make -j$(nproc) \
        TARGET=linux-glibc \
        USE_GETADDRINFO=1 \
        USE_OPENSSL=1 \
        USE_PCRE2=1 \
        USE_ZLIB=1 \
        PREFIX=/usr \
        SBINDIR=/usr/bin
      make \
        TARGET=linux-glibc \
        USE_GETADDRINFO=1 \
        USE_OPENSSL=1 \
        USE_PCRE2=1 \
        USE_ZLIB=1 \
        PREFIX=/usr \
        SBINDIR=/usr/bin \
        DESTDIR="${{targets.destdir}}" \
        install-bin

  - uses: strip

test:
  environment:
    contents:
      packages:
        - apk-tools
        - busybox
  pipeline:
    - runs: |
        haproxy -vv | grep -q "HAProxy version ${{package.version}}"
        apk info --who-owns /usr/bin/haproxy | grep -F "${{package.name}}-${{package.full-version}}"
