contents:
  repositories:
    - https://packages.wolfi.dev/os
  keyring:
    - https://packages.wolfi.dev/os/wolfi-signing.rsa.pub
  packages:
    - ca-certificates-bundle
    - haproxy-ingress

entrypoint:
  command: /usr/bin/haproxy-ingress-controller

environment:
  PATH: /usr/bin:/bin
